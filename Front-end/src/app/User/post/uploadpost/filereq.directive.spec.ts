import { FilereqDirective } from './filereq.directive';

describe('FilereqDirective', () => {
  it('should create an instance', () => {
    const directive = new FilereqDirective();
    expect(directive).toBeTruthy();
  });
});
